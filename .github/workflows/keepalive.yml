name: Keep Render Backend Awake

on:
  schedule:
    - cron: "*/10 * * * *"   # runs every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to Render URL
        run: |
          curl -I https://smart-task-analyser.onrender.com || true
